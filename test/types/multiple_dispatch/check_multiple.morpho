
class A {}

class B {}

fn f(A x, A y) { return 1 }

fn f(A x, B y) { return 2 }

fn f(x, y) { return 1.5 }

print f(A(), A()) // expect: 1

print f(0, 0)      // expect: 1.5

print f(A(), 1)   // expect: 1.5

print f(A(), B()) // expect: 2
