
import meshtools

var m = LineMesh(fn (t) [cos(t), sin(t)], -Pi...Pi:Pi/2, closed=true)
/*var mb = MeshBuilder()
mb.addvertex([0,0])
mb.addvertex([1,0])
mb.addedge([0,1])
var m = mb.build()*/

print m.vertexmatrix()

var a = Length()
print a.total(m)

print a.hessian(m)
