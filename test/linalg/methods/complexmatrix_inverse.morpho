// Inverse

var A = ComplexMatrix(2,2)
A[0,0]=0+0im
A[0,1]=1+im
A[1,0]=1-im
A[1,1]=0+0im

var Ainv = ComplexMatrix([[0, 0.5+0.5im],[0.5-0.5im, 0]])

print (A.inverse() - Ainv).norm() < 1e-10 // expect: true

