// Typed overloads dominate untyped wildcard overloads

class A {}

class B is A {}

class C {}

fn f(x) {
    return "_"
}

fn f(A x) {
    return "A"
}

fn f(B x) {
    return "B"
}

print f(A())
// expect: A

print f(B())
// expect: B

print f(C())
// expect: _

