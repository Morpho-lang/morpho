// Apply a function to a list of arguments

fn f(x,y) {
  return x^2+y^2
}

print apply(f, [0.5,0.5])
// expect: 0.5

var a = [0.1, 0.1]

print apply(f, a)
// expect: 0.02

fn g(x) {
  print x
}

apply(g, [[1,2,3]])
// expect: [ 1, 2, 3 ]
