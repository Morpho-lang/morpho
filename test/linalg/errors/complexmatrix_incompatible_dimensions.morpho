// Incompatible dimensions error

var A = ComplexMatrix(2,2)
A[0,0]=1+1im

var B = ComplexMatrix(3,3)
B[0,0]=1+1im

// Try to add incompatible matrices
print A + B
// expect error 'LnAlgMtrxIncmptbl'

