// Import modules into a namespace

import color, graphics, plot
import histogram as hist 
import color as col for Red, Green, Blue

//import color as col for Red, Green, Blue
//print col.Red
// expect: Red