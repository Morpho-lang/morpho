var list = [1,0.1,0.2,0.5,0.3]

print min(list) // expect: 0.1
print max(list) // expect: 1

list.sort()
print list
// expect: [ 0.1, 0.2, 0.3, 0.5, 1 ]

list.sort(fn (a, b) b-a)
print list
// expect: [ 1, 0.5, 0.3, 0.2, 0.1 ]
