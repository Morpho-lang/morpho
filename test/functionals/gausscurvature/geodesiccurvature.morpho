// Geodesic curvature 
import constants

// Import a disk mesh
var mesh = Mesh("disk.mesh")

mesh.addgrade(1)

var bnd = Selection(mesh, boundary=true)

// Geodesic curvature
var lmc = GaussCurvature()
lmc.geodesic = true
print abs(lmc.total(mesh, selection=bnd)-2*Pi)<1e-6
// expect: true
