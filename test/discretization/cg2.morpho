
import meshtools 

var m = LineMesh(fn (t) [t,0,0], 0..1:0.5) 

var l = FunctionSpace("CG2", grade=1)

var f = Field(m, functionspace=l)
for (i in 0...5) f[i] = i

print f 

print l.layout(f)

print f.shape()

//print LineIntegral(fn (x, u) u, f).total(m)
