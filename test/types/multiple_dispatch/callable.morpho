// Dispatch on Callable

fn f(Callable g, x) {
    print g(x)
}

fn f(Float x, y) {
    print x+y
}

fn h(x) { return 1 }

f(h, 0)   // expect: 1
f(cos, 0) // expect: 1

f("H", 0) // expect error 'MltplDsptchFld'
