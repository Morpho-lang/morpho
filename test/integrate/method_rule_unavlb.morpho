// Raise an err if a quadrature rule can't be found

import meshtools

var m1 = LineMesh(fn (t) [t],     0..1:0.5)

var a = LineIntegral(fn (x) x[0], method={ "degree" : 10000 }).total(m1)
// expect error 'IntgrtrRlUnavlb'
