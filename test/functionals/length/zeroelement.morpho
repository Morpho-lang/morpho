import meshtools

var mb = MeshBuilder()
mb.addvertex([0, 0, 0])
mb.addvertex([0, 0, 0])
mb.addedge([0, 1])
var m = mb.build()

var a = Length()

print a.total(m)
// expect: 0

print a.integrand(m)[0]
// expect: 0

print a.gradient(m)
// expect: [ 0 0 ]
// expect: [ 0 0 ]
// expect: [ 0 0 ]
