import constants
import meshtools

// Symmetries
var np=10

var m = LineMesh(fn (t) [0.5*cos(t), 0.5*sin(t),0], 0...2*Pi:2*Pi/np, closed=true)
print m

// Create the manifold
var lc = LineCurvatureSq()

print lc.integrand(m)
