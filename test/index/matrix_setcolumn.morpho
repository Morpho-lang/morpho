// Set columns of a Matrix

import newlinalg 

var A = XMatrix(2,2)

var b = XMatrix(2,1)
b[0] = 1
b[1] = 3

var c = XMatrix(2,1)
c[0] = 2
c[1] = 4

A.setColumn(0,b)
A.setColumn(1,c)

print A
// expect: [ 1 2 ]
// expect: [ 3 4 ]

print A.setColumn(2,b) // expect error 'LnAlgMtrxIndxBnds'
