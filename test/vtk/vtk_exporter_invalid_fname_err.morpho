import vtk
import meshtools 

var m = AreaMesh(fn (u,v) [u,v,0], -1..1:2, -1..1:2)
var f = Field(m, fn(x,y,z) x)

var vtkE = VTKExporter(f, fieldname="x y") // Expect: Error 'InvalidFnameErr' : fieldname (`x y`) cannot have embedded whitespaces.
