// Multiple dispatch with inheritance

class A {}

class B is A {} 

class C is B {} 

fn f(A x) { return "A" }

fn b(B x) { return "B" }

print f(A()) // expect: A 

print f(B()) // expect: B

print f(C()) // expect: B